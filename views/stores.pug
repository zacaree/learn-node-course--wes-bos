extends layout

include mixins/_storeCard

block content
  .inner
    h2 #{title}
    //- pre #{h.dump(stores)}
    .stores
      each store in stores
        +storeCard(store)
        //- .store
        //-   p #{store.name}
        //-   p #{store.description}
        //-   div
        //-     each tag in store.tags
        //-       p #{tag}
